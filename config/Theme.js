const colors = {
    primary: '#4a4a4a', 
    bg: '#F5F5F5', 
    grey_dark: '#A9A9A9',
    grey_shade_1: '#dbdbdb',
    white: '#FFFFFF'

  };
  
  const transitions = {
    normal: '1s ease',
  };
  
  const fontSize = {
    small: '0.9rem',
  };
  
  const theme = {
    colors,
    transitions,
    fontSize,
  };
  
  export default theme;
  