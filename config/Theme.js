const colors = {
    primary: '#e67070', 
    bg: '#F5F5F5', 
  };
  
  const transitions = {
    normal: '1s ease',
  };
  
  const fontSize = {
    small: '0.9rem',
  };
  
  const theme = {
    colors,
    transitions,
    fontSize,
  };
  
  export default theme;
  